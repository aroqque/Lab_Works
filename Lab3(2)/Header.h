#include <iostream>
#include <time.h>
#include <Windows.h>

using namespace std;

void fill(int* arr, int size);
void print(int* arr, int size);
void check(int* arr, int size);
void transfer(int* arr, int size, int* brr);