#pragma once
#include <iostream>
#include <string.h>
#include <windows.h>
using namespace std;
int count_words(const string& str);
int stretch(string& output, const string& input, const int target_size);