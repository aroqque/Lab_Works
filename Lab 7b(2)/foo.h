#pragma once
#include <iostream>
#include <windows.h>
using namespace std;
void fill_array(int*, const int);
void print_array(int*, const int);
void foo(char*, int*, const int);
void sum_foo(char*, int*, int*, const int);